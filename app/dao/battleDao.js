var pomelo = require('pomelo');
var utils = require('../util/utils');

var battleDao = module.exports;

battleDao.getBattleData = function(username,cb){
};

battleDao.setBattleData = function(username,battledata,cb){
};
